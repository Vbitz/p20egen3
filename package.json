{
  "dependencies": {
    "@types/node": "^8.5.1",
    "tslint": "^5.8.0",
    "tslint-language-service": "^0.9.7",
    "typescript": "^2.6.2"
  },
  "devDependencies": {
    "gts": "^0.5.2",
    "typescript": "^2.6.1"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  }
}
